# ===========================================
# 练习 5：综合项目 — 代码学习笔记生成器（⭐⭐⭐⭐）
# ===========================================
#
# 这是第一周的毕业项目，需要你从零独立完成。
# 不要看之前的教程代码，全凭自己写。
#
# 【项目需求】
#
# 做一个命令行工具，功能如下：
# 1. 扫描指定目录下的所有 .py 文件
# 2. 让 AI 为每个文件生成学习笔记（用 asyncio 并行调用）
# 3. 学习笔记以 JSON 格式返回，包含:
#    {
#      "file": "文件名",
#      "summary": "一句话总结",
#      "key_concepts": ["知识点1", "知识点2"],
#      "difficulty": "beginner/intermediate/advanced",
#      "js_comparison": "和前端的什么概念类似"
#    }
# 4. 汇总所有文件的笔记，生成一份 Markdown 报告保存到文件
# 5. 支持命令行参数指定目录（或默认当前目录）
#
# 【要求用到的知识点】
#
# - 文件读写 (Path, open, json)
# - 类和模块拆分 (至少 2 个类)
# - API 调用 (anthropic SDK)
# - 异步并发 (asyncio.gather + AsyncAnthropic)
# - 装饰器 (至少用 @timer)
# - 异常处理 (try/except)
# - 结构化输出 (让 AI 返回 JSON)
# - 环境变量 (.env)
#
# 【项目结构建议】
#
# week1-practice/
# ├── practice_05_project.py  ← 你现在在这里，写主入口
# ├── note_generator/
# │   ├── __init__.py
# │   ├── config.py          ← 配置管理
# │   ├── scanner.py         ← 文件扫描
# │   └── generator.py       ← AI 笔记生成
# └── output/
#     └── notes_report.md    ← 生成的报告
#
# 【评判标准】
#
# ✅ 能独立跑通，不报错
# ✅ 代码拆分成多个文件，不是全写在一个文件里
# ✅ 有异常处理，API 挂了不会崩溃
# ✅ 用了 asyncio 并行调用（不是串行）
# ✅ 生成的 Markdown 报告格式清晰可读
#
# 【开始吧！下面是你的代码区域】
# ===========================================
